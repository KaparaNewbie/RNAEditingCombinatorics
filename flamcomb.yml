name: flamcomb

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python = 3.6.7
  - star >= 2.5.4b  # STARlong
  - subread = 1.6.0  # FeatureCounts (see https://subread.sourceforge.net/)
  - regex = 2018.2.21
  - pysam = 0.14
  - pandas = 0.23.4
  - yaml = 0.1.7
